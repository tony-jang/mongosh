import connect from './connect';
import disconnect from './disconnect';
import eval from './eval';
import parse from './parse';
import read from './read';

export default { connect, disconnect, eval, parse, read };
